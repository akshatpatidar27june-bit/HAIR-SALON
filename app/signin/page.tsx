'use client';
import {FormEvent,useState} from 'react';import {ArrowLeft,LockKeyhole,Mail} from 'lucide-react';import Link from 'next/link';
export default function SignIn(){const [role,setRole]=useState('staff');const submit=(e:FormEvent)=>{e.preventDefault();alert('Authentication backend will be connected to the Render service next.');};return <main className="min-h-screen bg-[#f7f2ea] px-6 py-8"><Link href="/" className="inline-flex items-center gap-2 text-sm text-[#756b62]"><ArrowLeft size={16}/> Home</Link><div className="mx-auto flex min-h-[85vh] max-w-md items-center"><div className="w-full rounded-[32px] border border-[#e5ddd2] bg-white p-7 shadow-xl md:p-9"><div className="mb-8"><div className="mb-5 grid h-12 w-12 place-items-center rounded-2xl bg-[#17130f] font-bold text-white">HS</div><h1 className="serif text-3xl font-bold">Welcome back</h1><p className="mt-2 text-sm text-[#756b62]">Sign in to your salon workspace.</p></div><div className="mb-6 grid grid-cols-2 rounded-2xl bg-[#f7f2ea] p-1"><button onClick={()=>setRole('staff')} className={`rounded-xl py-3 text-sm font-semibold ${role==='staff'?'bg-white shadow':''}`}>Staff</button><button onClick={()=>setRole('owner')} className={`rounded-xl py-3 text-sm font-semibold ${role==='owner'?'bg-white shadow':''}`}>Owner / Manager</button></div><form onSubmit={submit} className="space-y-4"><label className="block text-sm font-semibold">Email<div className="mt-2 flex items-center gap-3 rounded-2xl border border-[#e5ddd2] px-4"><Mail size={17} className="text-[#756b62]"/><input required type="email" placeholder="you@salon.com" className="w-full bg-transparent py-4 outline-none"/></div></label><label className="block text-sm font-semibold">Password<div className="mt-2 flex items-center gap-3 rounded-2xl border border-[#e5ddd2] px-4"><LockKeyhole size={17} className="text-[#756b62]"/><input required type="password" placeholder="••••••••" className="w-full bg-transparent py-4 outline-none"/></div></label><button className="w-full rounded-2xl bg-[#17130f] py-4 font-semibold text-white shadow-lg">Sign in as {role==='owner'?'Owner / Manager':'Staff'}</button></form><p className="mt-6 text-center text-xs text-[#756b62]">Secure role-based access • Multi-outlet ready</p></div></div></main>}
